/** Pricing/limit constants for pay-as-you-go firing requests. */
export const FIRING_HALF_SHELF_CENTS = 2500
export const MAX_HALF_SHELVES = 8
export const MAX_FIRING_PHOTOS = 5
/** Per-photo upload size cap, shared by the client form and the server route. */
export const MAX_PHOTO_BYTES = 10 * 1024 * 1024
